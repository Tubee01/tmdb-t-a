version: '3.8'
services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  backend:
    build: .
    env_file:
      - .env
    environment:
      - REDIS_HOST=redis
      - NODE_ENV=production
    ports:
      - "3000:3000"
    depends_on:
      - redis

